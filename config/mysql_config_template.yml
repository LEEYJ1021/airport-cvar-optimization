# This file is a template for tools that might need it (e.g., Alembic).
# The primary source of truth for DB config is the .env file.
mysql:
  host: ${MYSQL_HOST}
  port: ${MYSQL_PORT}
  database: ${MYSQL_DB}
  user: ${MYSQL_USER}
  password: ${MYSQL_PASSWORD}